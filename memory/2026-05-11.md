# 2026-05-11 Daily Log

## PPN Tax Engine — LIVE 🚀

### What was done:
1. **Migration 1303** — Created 3 new tables:
   - `org_tax_settings` (PKP profile, NPWP, tarif PPN, auto-post toggle)
   - `tax_invoice_numbers` (NSFP range tracking)
   - `tax_invoices` (Faktur Pajak records)

2. **Database Functions:**
   - `get_spt_ppn_1111(p_org_id, p_tax_period)` — Hitung PPN Keluaran/Masukan per bulan
   - `create_tax_payment_journal(p_org_id, p_tax_period, p_paid_at, p_paid_from_account_id, p_notes)` — Auto-generate payment JE

3. **Server Actions** (`modules/accounting/actions/tax.actions.ts`):
   - `getOrgTaxSettings` / `upsertOrgTaxSettings`
   - `getSptPpn1111` / `downloadSptCsv` (CSV export SPT Masa 1111)
   - `payTax` (action wrapper for create_tax_payment_journal)
   - `generateTaxInvoice` / `getTaxInvoices`

4. **UI Updates** (TaxClient.tsx):
   - Bayar Pajak button → Modal: pilih rekening, auto-JE
   - Download SPT → Live CSV download
   - Settings gear icon → PKP setting modal (NPWP, tarif 11%/12%, toggle PKP)
   - Toast notification system (success/error)  
   - All "Segera Hadir" stubs replaced

5. **Version log updated** — Patch entry: PPN engine

### Still open:
- Activation redirect back to /marketplace (need end-to-end verification)
- Server Component error on marketplace page
- Inventory save error
- Manufacturing/Workshop/Job Order CoA injection functions

### Commit: `7cfeeac` — pushed to origin/main
