'use client'

import { SetupClient } from './setup-client'

/**
 * Fully client-side setup page.
 * Zero server component logic. Zero server action imports in client.
 */
export default function ModuleSetupPage() {
  return <SetupClient />
}
